# FPSE_f25_project

This project is an implementation of Texas Hold 'Em Poker in OCaml. The user interacts with the game via terminal and can go against bot players controlled by algorithmic AI.

To build and run this project, simply run "dune build" and then "dune exec poker."

