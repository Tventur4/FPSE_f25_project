player.mli
  - name
  - player_id (for in-game)
  - chip stack
  - hole cards
  - functions:
    - constructors
    - getters (all attributes)
    - setters/manipulators (chip stack, hole cards)

hand.mli?
  - cards
  - value (i.e., high card -> 0, royal flush -> 9)
  - maybe include as type in player.mli
  - functions:
    - constructors
    - getters (all attributes)
    - setters (cards -> sets value)
    - get_best_hand (input: 7 cards--2 hole cards and 5 community cards)
    - compare
    - to_string?

game.mli?
  - state (.mli? or just a type in game.mli?)
    - hand number
    - betting round
  - players
    - maybe include this in state but need to keep track of:
      - order of players
      - whose still in (i.e. has not folded) on a given betting round
      - big/small blinds
  - deck
  - community cards
  - pot (.mli?)
    - could include attributes like:
      - pot value
      - value of big/small blind
      - minimum bet/raise values
      - maximum raise value
  - functions:
    - depends on how we implement state, pot, and other gameplay aspects
